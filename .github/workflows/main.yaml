name: Model Training Pipeline

on: [push] # Chạy mỗi khi có code mới được push

jobs:
  run-training:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      # 1. Cài đặt Python & CML
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - uses: iterative/setup-cml@v1

      # 2. Cài đặt thư viện
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install dvc[gdrive] # Nhớ cài gói hỗ trợ gdrive

      # 3. TẠO FILE CREDENTIAL TỪ SECRET (Bước quan trọng nhất)
      # GitHub Runner sẽ lấy nội dung từ Secret và tạo lại file json
     - name: Setup DVC (Service Account Method - Best Practice)
        env:
          GDRIVE_CREDENTIALS_DATA: ${{ secrets.GDRIVE_CREDENTIALS_DATA }}
        run: |
          # 1. Tạo file Service Account từ Secret
          echo "$GDRIVE_CREDENTIALS_DATA" > service-account.json

          # 2. XÓA SẠCH cấu hình OAuth cũ (để tránh xung đột)
          # (DVC sẽ ưu tiên Service Account nếu cấu hình đúng, nhưng xóa đi cho chắc)
          dvc remote modify --local myremote --unset gdrive_client_id || true
          dvc remote modify --local myremote --unset gdrive_client_secret || true
          dvc remote modify --local myremote --unset gdrive_user_credentials_file || true
          dvc remote modify --local myremote --unset gdrive_authenticator || true

          # 3. KÍCH HOẠT chế độ Service Account
          dvc remote modify --local myremote gdrive_use_service_account true
          dvc remote modify --local myremote gdrive_service_account_json_file_path service-account.json

          # 4. Kiểm tra kết nối (Debug)
          echo "Đang thử kết nối Drive bằng Service Account..."
          dvc ls myremote || echo "⚠️ Nếu lỗi, hãy kiểm tra xem bạn đã Share Folder Drive cho email service account chưa?"

      # 4. Kéo dữ liệu về (nếu cần) và Chạy Pipeline
      - name: Run DVC Pipeline
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Pull dữ liệu về (nếu data gốc lưu trên Drive)
          dvc pull 
          
          # Chạy training
          dvc repro 

      # 5. Báo cáo kết quả (CML Report)
      - name: Write CML Report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "## Model Metrics" >> report.md
          
          # Giả sử lệnh dvc metrics diff in ra bảng so sánh
          dvc metrics diff --show-md >> report.md
          
          # Đăng comment vào bài commit/PR
          cml comment create report.md